{{#each domains as |domain id|}}
    <div class="domain-row" data-item-id="{{domain._id}}">
        <div class="domain-name">• {{domain.name}}</div>
        {{#if system.descVisible}}
            <a class="domain-desc" data-action="hide" title="{{localize 'dune2d20.hint.hideDomainDesc'}}"><i class="fa-solid fa-caret-up"></i></a>
        </div>
           <div class="desc-row">{{editor system.description target="system.description" button=false owner=true editable=false}}</div>
        {{else}}
            <a class="domain-desc" data-action="show" title="{{localize 'dune2d20.hint.showDomainDesc'}}"><i class="fa-solid fa-caret-down"></i></a>
        </div>
        {{/if}}
{{/each}}