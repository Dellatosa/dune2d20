{{#each domains as |domain id|}}
    <div class="domain-row" data-item-id="{{domain._id}}">
        <div class="domain-name">• {{domain.name}}</div>
        <a class="edit-domain" title="{{localize 'dune2d20.hint.editDomain'}}"><i class="fa-regular fa-sliders"></i></a>
        <a class="remove-domain" title="{{localize 'dune2d20.hint.removeDomain'}}"><i class="fa-solid fa-hexagon-xmark"></i></a>
    </div>
{{/each}}