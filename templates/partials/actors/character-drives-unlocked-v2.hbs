<!-- Duty -->
<div class="drive">{{ localize 'dune2d20.actor.duty' }}</div>
<div class="val"><input name="system.drives.duty.value" type="number" value="{{system.drives.duty.value}}" min="{{system.drives.duty.min}}" max="{{system.drives.duty.max}}"/></div>
<div class="statement"><textarea name="system.drives.duty.statement">{{system.drives.duty.statement}}</textarea></div>
<div class="stat-end"> </div>
<!-- Faith -->
<div class="drive">{{ localize 'dune2d20.actor.faith' }}</div>
<div class="val"><input name="system.drives.faith.value" type="number" value="{{system.drives.faith.value}}" min="{{system.drives.faith.min}}" max="{{system.drives.faith.max}}"/></div>
<div class="statement"><textarea name="system.drives.faith.statement">{{system.drives.faith.statement}}</textarea></div>
<div class="stat-end"> </div>
<!-- Justice -->
<div class="drive">{{ localize 'dune2d20.actor.justice' }}</div>
<div class="val"><input name="system.drives.justice.value" type="number" value="{{system.drives.justice.value}}" min="{{system.drives.justice.min}}" max="{{system.drives.justice.max}}"/></div>
<div class="statement"><textarea name="system.drives.justice.statement">{{system.drives.justice.statement}}</textarea></div>
<div class="stat-end"> </div>
<!-- Power -->
<div class="drive">{{ localize 'dune2d20.actor.power' }}</div>
<div class="val"><input name="system.drives.power.value" type="number" value="{{system.drives.power.value}}" min="{{system.drives.power.min}}" max="{{system.drives.power.max}}"/></div>
<div class="statement"><textarea name="system.drives.power.statement">{{system.drives.power.statement}}</textarea></div>
<div class="stat-end"> </div>
<!-- Truth -->
<div class="drive">{{ localize 'dune2d20.actor.truth' }}</div>
<div class="val"><input name="system.drives.truth.value" type="number" value="{{system.drives.truth.value}}" min="{{system.drives.truth.min}}" max="{{system.drives.truth.max}}"/></div>
<div class="statement"><textarea name="system.drives.truth.statement">{{system.drives.truth.statement}}</textarea></div>
<div class="stat-end"> </div>