{{#each assets as |asset id|}}
    <div class="asset-row" data-item-id="{{asset._id}}">
        <div class="asset-name">• {{asset.name}}</div>
        {{#if system.descVisible}}
            <a class="asset-desc" data-action="hide" title="{{localize 'dune2d20.hint.hideAssetDesc'}}"><i class="fa-solid fa-caret-up"></i></a>
        </div>
           <div class="desc-row">{{editor system.description target="system.description" button=false owner=true editable=false}}</div>
        {{else}}
            <a class="asset-desc" data-action="show" title="{{localize 'dune2d20.hint.showAssetDesc'}}"><i class="fa-solid fa-caret-down"></i></a>
        </div>
        {{/if}}
{{/each}}