<!-- Duty -->
<div class="drive">{{ localize 'dune2d20.actor.duty' }} <a data-action="rollDrive" title="{{localize 'dune2d20.hint.rollDrive'}}" data-drive="duty"><i class="fa-solid fa-dice-d20"></i></a></div>
<div class="val"><input name="system.drives.duty.value" type="number" value="{{system.drives.duty.value}}" min="{{system.drives.duty.min}}" max="{{system.drives.duty.max}}" readonly/></div>
<div class="statement"><textarea name="system.drives.duty.statement" readonly>{{system.drives.duty.statement}}</textarea></div>
<div class="stat-end"> </div>
<!-- Faith -->
<div class="drive">{{ localize 'dune2d20.actor.faith' }} <a data-action="rollDrive" title="{{localize 'dune2d20.hint.rollDrive'}}" data-drive="faith"><i class="fa-solid fa-dice-d20"></i></a></div>
<div class="val"><input name="system.drives.faith.value" type="number" value="{{system.drives.faith.value}}" min="{{system.drives.faith.min}}" max="{{system.drives.faith.max}}" readonly/></div>
<div class="statement"><textarea name="system.drives.faith.statement" readonly>{{system.drives.faith.statement}}</textarea></div>
<div class="stat-end"> </div>
<!-- Justice -->
<div class="drive">{{ localize 'dune2d20.actor.justice' }} <a data-action="rollDrive" title="{{localize 'dune2d20.hint.rollDrive'}}" data-drive="justice"><i class="fa-solid fa-dice-d20"></i></a></div>
<div class="val"><input name="system.drives.justice.value" type="number" value="{{system.drives.justice.value}}" min="{{system.drives.justice.min}}" max="{{system.drives.justice.max}}" readonly/></div>
<div class="statement"><textarea name="system.drives.justice.statement" readonly>{{system.drives.justice.statement}}</textarea></div>
<div class="stat-end"> </div>
<!-- Power -->
<div class="drive">{{ localize 'dune2d20.actor.power' }} <a data-action="rollDrive" title="{{localize 'dune2d20.hint.rollDrive'}}" data-drive="power"><i class="fa-solid fa-dice-d20"></i></a></div>
<div class="val"><input name="system.drives.power.value" type="number" value="{{system.drives.power.value}}" min="{{system.drives.power.min}}" max="{{system.drives.power.max}}" readonly/></div>
<div class="statement"><textarea name="system.drives.power.statement" readonly>{{system.drives.power.statement}}</textarea></div>
<div class="stat-end"> </div>
<!-- Truth -->
<div class="drive">{{ localize 'dune2d20.actor.truth' }} <a data-action="rollDrive" title="{{localize 'dune2d20.hint.rollDrive'}}" data-drive="truth"><i class="fa-solid fa-dice-d20"></i></a></div>
<div class="val"><input name="system.drives.truth.value" type="number" value="{{system.drives.truth.value}}" min="{{system.drives.truth.min}}" max="{{system.drives.truth.max}}" readonly/></div>
<div class="statement"><textarea name="system.drives.truth.statement" readonly>{{system.drives.truth.statement}}</textarea></div>
<div class="stat-end"> </div>